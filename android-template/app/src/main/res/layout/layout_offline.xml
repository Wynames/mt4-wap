<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0a0a0b"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp">

    <!-- Icon indicating connection loss (optional, using a simple text symbol for now) -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="⚡"
        android:textSize="48sp"
        android:textColor="#ffffff"
        android:layout_marginBottom="24dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Koneksi Terputus"
        android:textSize="24sp"
        android:textColor="#ffffff"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="8dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Mohon periksa jaringan internet Anda."
        android:textSize="16sp"
        android:textColor="#9ca3af"
        android:fontFamily="sans-serif"
        android:layout_marginBottom="32dp"
        android:gravity="center" />

    <Button
        android:id="@+id/retryButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Coba Lagi"
        android:textColor="#0a0a0b"
        android:background="@drawable/button_white_rounded"
        android:paddingHorizontal="32dp"
        android:paddingVertical="12dp"
        android:elevation="0dp"
        android:fontFamily="sans-serif-medium" />

    <!-- Promotional footer -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Want to build an app like this?\nVisit APK Builder"
        android:textSize="12sp"
        android:textColor="#4b5563"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:layout_marginTop="48dp"
        android:alpha="0.7" />

</LinearLayout>
